# ============================================================
# Swastik MCP — Dashboard Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git (it is already in .gitignore).

# ── Backend API ────────────────────────────────────────────
# ⚠  PORT INVARIANT: always 3939 for local dev.
#    For a production build deployed on Firebase Hosting, set this
#    to your Render backend URL.
VITE_API_BASE_URL=http://localhost:3939/api

# ── Firebase Web SDK ───────────────────────────────────────
# These are PUBLIC values — safe to expose in a browser bundle.
# Find them in: Firebase Console → Project Settings → General → Your apps
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
